- defaults:
    link:
        relink: true

- clean: ['~']

- shell:
    - [git submodule update --init --recursive, Installing/updating submodules]

- link:
    ~/.dotfiles: ''
    ~/.vim: vim
    ~/.vimrc: space-vim/init.vim
    ~/.space-vim: space-vim
    ~/.spacevim: spacevim

